{
  "name": "mterminal-marketplace",
  "version": "0.1.0",
  "private": true,
  "description": "marketplace backend, cli, and shared packages for mTerminal extensions",
  "license": "MIT",
  "packageManager": "pnpm@9.12.3",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "dev:worker": "pnpm --filter @mterminal/worker dev",
    "smoke": "bash scripts/smoke.sh"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
